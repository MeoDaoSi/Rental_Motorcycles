const roleEnum = {
    USER: "USER",
    ADMIN: "ADMIN",
}

const genderEnum = {
    F: "F",
    M: "M",
}

const statusEnum = {
    ACTIVE: "ACTIVE",
    INACTIVE: "INACTIVE",
    BANNED: "BANNED",
}

module.exports = {
    roleEnum,
    genderEnum,
    statusEnum,
}